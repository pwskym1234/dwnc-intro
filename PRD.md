# PRD — 방우식 자기소개 페이지 (DWNC 10기 1차 과제)

## 1. 한 줄 정의
DWNC 10기 부원과 지인이 링크 하나로 방우식이 누구인지 1분 안에 파악하고, 방명록에 한마디를 남길 수 있는 원페이지 사이트.

## 2. 누가 쓰는가
- 1차 코딩데이 쇼케이스에서 내 URL을 받은 DWNC 10기 부원. 모바일로 열어 30초 훑고 방명록에 한 줄 남긴다.
- 과제를 검토하는 교육팀 운영진. 방명록이 실제로 동작하는지(등록 → 새로고침 후 유지) 확인한다.

## 3. 사용 순서
1. URL 접속 → Hero에서 이름과 한 줄 소개를 본다.
2. 스크롤 → 관심사 / 해온 것 / DWNC에서 만들고 싶은 것 / 연락처 카드를 본다.
3. 페이지 하단 방명록에서 이름과 메시지를 입력하고 "남기기"를 누른다.
4. 목록 맨 위에 방금 쓴 글이 바로 보인다. 새로고침해도 남아 있다.

## 4. 만들 기능
- 자기소개 섹션 4개 (Hero, About/관심사, Projects, DWNC 목표 + 연락처)
- 방명록: 이름 + 메시지 등록, 최신순 목록, 빈 입력·길이 초과 차단, 등록 직후 목록 갱신
- 모바일 반응형

## 5. 화면 (단일 페이지)
- GNB: 이름 로고 + 섹션 앵커 링크 (About / Projects / Guestbook)
- Hero: 이름, 한 줄 소개, 소속 태그, CTA(방명록으로 이동)
- About: 관심사·취미 카드 그리드
- Projects: 해온 것 / 만들어본 것 카드 (제목, 한 줄 설명, 태그)
- DWNC: 이 동아리에서 만들고 싶은 것
- Contact: 이메일, GitHub 링크
- Guestbook: 입력 폼(이름, 메시지) + 목록(이름, 메시지, 시각)
- Footer

디자인: 밝은 미니멀. 흰 배경, 딥블루(#1e3a8a 계열) 포인트 컬러, 카드 레이아웃, 넉넉한 여백, 큰 타이포.

## 6. 저장할 내용
Supabase 테이블 `guestbook`
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | uuid | PK, 자동 |
| name | text | 1~30자 |
| message | text | 1~300자 |
| created_at | timestamptz | 자동 |

RLS: anon 읽기/쓰기 허용, 수정·삭제 불가.

## 7. 이상한 상황에서는
- 방명록이 비어 있으면 "첫 번째 방명록을 남겨주세요" 안내.
- 이름/메시지가 공백이면 등록하지 않고 인라인 에러 표시.
- 길이 초과(이름 30자, 메시지 300자)면 등록하지 않고 안내.
- Supabase 오류 시 "잠시 후 다시 시도해주세요" 표시, 페이지는 깨지지 않음.

## 8. 안 만들 것 (Non-goals)
- 로그인 / 회원가입
- 방명록 수정·삭제·좋아요
- 다국어, 블로그, CMS
- 이미지 업로드

## 기술 스택
Next.js 16 (App Router) + TypeScript + Tailwind CSS v4 + Supabase (supabase-js) + GitHub + Vercel
